<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.trifling.things.repository.UserMapper">

    <insert id="save">
        INSERT INTO User
        (userid, password, email, gender, age)
        Values(#{userId}, #{password}, #{email} ,#{gender}, #{age})
    </insert>

    <select id="findUser" resultType="user">
        select
        (userid, password, email, gender, age)
        from m_user
        WHERE userid = #{userid}
    </select>

    <update id="change">
        UPDATE User
        SET password = #{password}, email=#{email}
        where userid = #{userid}
    </update>

    <select id="isDuplicate" resultType="int">
        SELECT COUNT(*)
        FROM m_user
        <if test="type == 'userid'">
            WHERE userid = #{keyword}
        </if>
        <if test="type == 'email'">
            WHERE email = #{keyword}
        </if>
    </select>


</mapper>